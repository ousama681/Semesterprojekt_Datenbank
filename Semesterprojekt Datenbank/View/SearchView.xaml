<UserControl x:Class="Semesterprojekt_Datenbank.View.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Semesterprojekt_Datenbank.Viewmodel"
             mc:Ignorable="d" 
            d:DesignHeight="650" d:DesignWidth="1280">

    <UserControl.DataContext>
        <vm:MainWindowVm/>
    </UserControl.DataContext>

    <Grid Margin="0,70,0,60"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch"
          Visibility="Visible">
        <DataGrid x:Name="DGSearch" d:ItemsSource="{d:SampleData ItemCount=10}"/>
        <TextBox x:Name="TXTTest" HorizontalAlignment="Left" Margin="230,402,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="120"/>

    </Grid>
</UserControl>
